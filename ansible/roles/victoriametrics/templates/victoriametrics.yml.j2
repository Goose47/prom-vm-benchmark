scrape_configs:
  - job_name: 'nodes'
    static_configs:
      - targets: 
        {% for n in range(victoriametrics_host_number) %}
        - node-{{ n }}:9200
        labels:
            host_number: {{ n }}
            environment: production
        {% endfor %}
